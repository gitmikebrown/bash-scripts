# Auto detect text files and perform LF normalization
* text=auto

# Force LF line endings for shell scripts
*.sh text eol=lf
*.bash text eol=lf

# Force LF for other common Unix files
*.conf text eol=lf
*.cfg text eol=lf

# Force CRLF for Windows files (if any)
*.bat text eol=crlf
*.cmd text eol=crlf

# Binary files (don't normalize)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.zip binary
*.tar.gz binary